export interface VisitLog {
    title: string;
    startDate: string;
    endDate: string;
    notes: string;

}

export interface VisitLogFormValues {
    title: string;
    startDate: string;
    endDate: string;
    notes: string;
}